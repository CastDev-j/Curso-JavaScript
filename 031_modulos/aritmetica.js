const aritmetica = {
    sumar: (a, b) => a + b,
    restar: (a, b) => a - b,
    multiplicar: (a, b) => a * b,
    dividir: (a, b) => a / b
}

export default aritmetica;

export {PII as PI2};

const PII = 3.1416;

